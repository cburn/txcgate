from setuptools import find_packages, setup
setup(name="txcgate",
      version="0.1.0",
      description="Clipsal C-Gate Protocol for Twisted",
      author="Chris Burn",
      author_email='chrisburn@fastmail.ney',
      platforms=["any"],
      packages=find_packages(),
      )
